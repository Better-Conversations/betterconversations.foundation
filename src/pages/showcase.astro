---
import Layout from '../layouts/Layout.astro';

// Blank profile picture SVG data URL
const blankProfilePicture = 'data:image/svg+xml,%3Csvg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200"%3E%3Ccircle cx="100" cy="100" r="100" fill="%23f3f4f6"/%3E%3Cpath d="M100 50c16.6 0 30 13.4 30 30s-13.4 30-30 30-30-13.4-30-30 13.4-30 30-30zm0 70c33.1 0 60 26.9 60 60v20H40v-20c0-33.1 26.9-60 60-60z" fill="%23d1d5db"/%3E%3C/svg%3E';

const ambassadors = [
  {
    id: 'lyndsay-conklin',
    name: 'Lyndsay Conklin',
    role: 'BCF Ambassador',
    location: 'United States',
    specialties: ['Clean Language', 'Organizational Development', 'Executive Coaching'],
    bio: 'Lyndsay brings a unique blend of business acumen and communication expertise to her work with Fortune 500 companies. Her innovative approach to Clean Language has transformed how organizations approach strategic conversations and team alignment. ',
    impact: '200+ leaders coached',
    image: blankProfilePicture
  },
  {
    id: 'evelina-de-lain',
    name: 'Evelina De Lain',
    role: 'BCF Ambassador',
    location: 'United Kingdom',
    specialties: ['Emergent Knowledge', 'Creative Industries', 'Innovation Workshops'],
    bio: 'Evelina specializes in applying Better Conversations principles to creative and innovation processes. Her work with design teams and creative agencies has revolutionized how these organizations harness collective creativity.',
    impact: '100+ workshops delivered',
    image: blankProfilePicture
  },
  {
    id: 'virginia-mckendry',
    name: 'Virginia McKendry, Ph.D.',
    role: 'BCF Ambassador',
    location: 'Canada',
    specialties: ['Academic Research', 'Healthcare Communication', 'Evidence-Based Practice'],
    bio: 'Dr. McKendry bridges the gap between academic research and practical application. Her groundbreaking studies on communication in healthcare settings have improved patient outcomes and team collaboration across multiple institutions.',
    impact: '15 research publications',
    image: blankProfilePicture
  },
  {
    id: 'matthew-michaelsen',
    name: 'Matthew Michaelsen',
    role: 'BCF Ambassador',
    location: 'United States',
    specialties: ['Technology Teams', 'Agile Coaching', 'Remote Collaboration'],
    bio: 'Matthew has pioneered the integration of Better Conversations methodology into agile software development practices. His frameworks for improving communication in distributed tech teams have been adopted by startups and enterprises globally.',
    impact: '50+ tech companies',
    image: blankProfilePicture
  },
  {
    id: 'robyn-odegaard',
    name: 'Dr Robyn Odegaard',
    role: 'BCF Ambassador',
    location: 'United States',
    specialties: ['Leadership Development', 'Emotional Intelligence', 'Team Dynamics'],
    bio: 'Dr. Odegaard is renowned for her work in developing emotionally intelligent leaders. Her unique approach combines Better Conversations techniques with neuroscience insights to create transformative leadership programs.',
    impact: '1000+ leaders trained',
    image: blankProfilePicture
  },
  {
    id: 'dan-parsons',
    name: 'Dan Parsons',
    role: 'BCF Ambassador',
    location: 'United States',
    specialties: ['Clean Language', 'Executive Coaching', 'Team Dynamics'],
    bio: 'Dan has been instrumental in bringing Better Conversations methodology to Fortune 500 companies across Europe. His innovative approach to Clean Language in corporate settings has transformed how leadership teams communicate.',
    impact: '500+ executives trained',
    image: blankProfilePicture
  },
  {
    id: 'christine-stief',
    name: 'Christine Stief',
    role: 'BCF Ambassador',
    location: 'Germany',
    specialties: ['Academic Integration', 'Research', 'Youth Programs'],
    bio: 'Christine has pioneered the integration of Better Conversations principles into educational curricula. Her work with universities has created a new generation of communication-conscious professionals.',
    impact: '10 universities engaged',
    image: blankProfilePicture
  },
  {
    id: 'barb-collombin',
    name: 'Barb Collombin',
    role: 'BCF Ambassador',
    location: 'Canada',
    specialties: ['Community Building', 'Workshop Facilitation', 'Peer Learning'],
    bio: 'Barb has created thriving BCF communities across North America. Her innovative "Conversation Cafés" have become a model for grassroots engagement, bringing Better Conversations principles to diverse communities and organizations.',
    impact: '75+ community events',
    image: blankProfilePicture
  }
];

// Sort ambassadors alphabetically by last name
const sortedAmbassadors = [...ambassadors].sort((a, b) => {
  const lastNameA = a.name.split(' ').pop() || '';
  const lastNameB = b.name.split(' ').pop() || '';
  return lastNameA.localeCompare(lastNameB);
});
---

<Layout title="Ambassador & Partner Showcase - Better Conversations Foundation">
  <style>
    .showcase-container {
      scroll-snap-type: y mandatory;
      overflow-y: scroll;
      height: 100vh;
      position: relative;
    }
    
    .showcase-section {
      scroll-snap-align: start;
      min-height: 100vh;
      display: flex;
      align-items: center;
      position: relative;
    }
    
    .progress-indicator {
      position: fixed;
      right: 2rem;
      top: 50%;
      transform: translateY(-50%);
      z-index: 50;
    }
    
    .progress-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background-color: #e5e7eb;
      margin: 1rem 0;
      cursor: pointer;
      transition: all 0.3s ease;
      position: relative;
    }
    
    .progress-dot.active {
      background-color: #54C4B6;
      transform: scale(1.5);
    }
    
    .progress-dot:hover::after {
      content: attr(data-name);
      position: absolute;
      right: 20px;
      top: 50%;
      transform: translateY(-50%);
      background: #1f2937;
      color: white;
      padding: 0.25rem 0.75rem;
      border-radius: 0.375rem;
      white-space: nowrap;
      font-size: 0.875rem;
    }
    
    .card-enter {
      animation: cardFlip 0.6s ease-out;
    }
    
    @keyframes cardFlip {
      0% {
        transform: translateY(100px) rotateX(-15deg);
        opacity: 0;
      }
      100% {
        transform: translateY(0) rotateX(0);
        opacity: 1;
      }
    }
  </style>

  <!-- Progress Indicator -->
  <div class="progress-indicator hidden lg:block">
    <div class="progress-dot active" data-name="Introduction" data-section="hero"></div>
    {sortedAmbassadors.map((person) => (
      <div class="progress-dot" data-name={person.name.split(' ')[0]} data-section={person.id}></div>
    ))}
  </div>

  <div class="showcase-container">
    <!-- Hero Section -->
    <section id="hero" class="showcase-section bg-gradient-to-b from-white via-gray-50 to-white">
      <div class="absolute inset-0 bg-gradient-to-br from-[#54C4B6]/5 to-[#A8D381]/5"></div>
      <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 mb-6">
          Meet Our <span class="text-[#54C4B6]">Ambassadors</span> & <span class="text-[#A8D381]">Partners</span>
        </h1>
        <p class="text-xl md:text-2xl text-gray-600 mb-8 max-w-3xl mx-auto">
          Dedicated professionals around the world who champion Better Conversations methodology and create lasting impact in their communities.
        </p>
        <div class="flex items-center justify-center gap-2 text-gray-500">
          <svg class="w-5 h-5 animate-bounce" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
          </svg>
          <span>Scroll to explore</span>
        </div>
      </div>
    </section>

    <!-- Ambassador Cards -->
    {sortedAmbassadors.map((person, index) => (
      <section id={person.id} class="showcase-section">
        <div class="absolute inset-0 bg-gradient-to-br from-white to-gray-50"></div>
        <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
          <div class={`card-enter flex flex-col ${index % 2 === 0 ? 'lg:flex-row' : 'lg:flex-row-reverse'} items-center gap-12 lg:gap-16`}>
            <!-- Image Section -->
            <div class="flex-shrink-0 relative">
              <div class="absolute -inset-6 bg-gradient-to-r from-[#54C4B6]/20 to-[#A8D381]/20 rounded-full blur-3xl"></div>
              <div class={`absolute top-4 ${index % 2 === 0 ? 'left-4' : 'right-4'} w-[250px] h-[250px] bg-gradient-to-br from-[#54C4B6] to-[#A8D381] rounded-full opacity-20`}></div>
              <img 
                src={person.image} 
                alt={person.name} 
                class="relative rounded-full object-cover block shadow-2xl border-4 border-white" 
                style="width: 250px; height: 250px;" 
              />
              <div class="absolute -bottom-2 left-1/2 transform -translate-x-1/2 bg-white rounded-full px-4 py-2 shadow-lg">
                <p class="text-sm font-medium text-gray-700">{person.location}</p>
              </div>
            </div>

            <!-- Content Section -->
            <div class="flex-1 text-center lg:text-left">
              <div class="mb-6">
                <h2 class="text-4xl font-bold text-gray-900 mb-2">{person.name}</h2>
                <p class="text-xl text-[#54C4B6] font-medium">{person.role}</p>
              </div>

              <p class="text-lg text-gray-600 mb-6 leading-relaxed">
                {person.bio}
              </p>

              <div class="mb-6">
                <h3 class="text-sm font-semibold text-gray-500 uppercase tracking-wide mb-3">Specialties</h3>
                <div class="flex flex-wrap gap-2 justify-center lg:justify-start">
                  {person.specialties.map((specialty) => (
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-gradient-to-r from-[#54C4B6]/10 to-[#A8D381]/10 text-gray-700">
                      {specialty}
                    </span>
                  ))}
                </div>
              </div>

              <div class="flex flex-wrap gap-3 justify-center lg:justify-start">
                <div class="inline-flex items-center gap-2 px-4 py-2 bg-gradient-to-r from-[#54C4B6] to-[#A8D381] text-white rounded-lg shadow-sm">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                  <span class="font-medium">{person.impact}</span>
                </div>
                <a 
                  href={`https://www.linkedin.com/search/results/all/?keywords=${encodeURIComponent(person.name)}`} 
                  target="_blank" 
                  rel="noopener noreferrer"
                  class="inline-flex items-center gap-2 px-4 py-2 border border-gray-300 text-gray-700 bg-white rounded-lg shadow-sm hover:bg-gray-50 transition-colors"
                  title={`Connect with ${person.name} on LinkedIn`}
                >
                  <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/>
                  </svg>
                  <span class="font-medium">LinkedIn</span>
                </a>
              </div>
            </div>
          </div>
        </div>
      </section>
    ))}
  </div>

  <script>
    // Handle progress indicator clicks
    document.querySelectorAll('.progress-dot').forEach((dot, index) => {
      dot.addEventListener('click', () => {
        const sectionId = dot.getAttribute('data-section');
        if (sectionId) {
          const section = document.getElementById(sectionId);
          section?.scrollIntoView({ behavior: 'smooth' });
        }
      });
    });

    // Update active dot on scroll
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          document.querySelectorAll('.progress-dot').forEach((dot) => {
            dot.classList.remove('active');
            if (dot.getAttribute('data-section') === entry.target.id) {
              dot.classList.add('active');
            }
          });
        }
      });
    }, { threshold: 0.5 });

    document.querySelectorAll('.showcase-section').forEach((section) => {
      observer.observe(section);
    });

    // Add card animation on scroll
    const cardObserver = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.querySelector('.card-enter')?.classList.add('card-enter');
        }
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('.showcase-section:not(#hero)').forEach((section) => {
      cardObserver.observe(section);
    });
  </script>
</Layout>