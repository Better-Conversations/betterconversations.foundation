// Tailwind CSS v3 via PostCSS. This replaces the @astrojs/tailwind integration,
// which only supports Astro 5 and below (it wired up these same two plugins).
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
